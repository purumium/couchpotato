<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.kosa.dao.ReviewMapper">
	
	<!-- <insert id="insertReview" parameterType="com.kosa.dto.ReviewDTO">
        INSERT INTO reviews (USER_NUMBER, CONTENT_NAME, REVIEW_TEXT, RATING, CONTENT_IMAGE_URL,REVIEW_CREATE_AT)
        VALUES (#{userId},#{contentName},#{rating},#{reviewContent}, #{imgurl}, sysdate)
    </insert> -->
	
    
    <insert id="insertReview" parameterType="com.kosa.dto.ReviewDTO">
        INSERT INTO reviews (USER_NUMBER, CONTENT_NAME, REVIEW_TEXT, RATING, CONTENT_IMAGE_URL,REVIEW_CREATE_AT)
        VALUES (#{userId},#{contentName},#{reviewContent},#{rating},#{imgurl}, sysdate)
    </insert>
</mapper>
