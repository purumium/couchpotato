<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.kosa.mapper.MemberMapper">

    <!-- 회원가입 -->
    <insert id="memberJoin">
        INSERT INTO users (user_number, user_id, password, username, email, profile_picture_url, date_of_birth)
        VALUES (user_seq.NEXTVAL, #{user_id}, #{password}, #{username}, #{email}, #{profile_picture_url}, #{date_of_birth})
    </insert>

    <!-- 아이디 중복 검사 -->
    <select id="idCheck" resultType="int">
        SELECT COUNT(*) FROM users WHERE user_id = #{user_id}
    </select>

    <!-- 로그인 -->
    <select id="memberLogin" resultType="com.kosa.dto.MemberVO">
        SELECT user_id, password, email, profile_picture_url
        FROM users
        WHERE user_id = #{user_id}
    </select>

    <!-- 프로필 이미지 수정 -->
    <update id="updateMypage" parameterType="com.kosa.dto.MemberVO">
        UPDATE users
        SET profile_picture_url = #{profile_picture_url}
        WHERE user_id = #{user_id}
    </update>

    <!-- 비밀번호 변경 -->
    <update id="updatePassword" parameterType="com.kosa.dto.MemberVO">
        UPDATE users
        SET password = #{password}
        WHERE user_id = #{user_id}
    </update>

    <!-- 회원탈퇴 -->
    <delete id="memberDelete" parameterType="com.kosa.dto.MemberVO">
        DELETE FROM users
        WHERE user_id = #{user_id}
    </delete>
</mapper>